# URL of the BFF (apps/api). EXPO_PUBLIC_ is required to expose to the client.
# iOS simulator can use localhost. Physical devices need the dev machine's LAN IP.
EXPO_PUBLIC_API_URL=http://localhost:4000
