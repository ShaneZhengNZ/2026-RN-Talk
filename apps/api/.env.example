# Optional: a GitHub personal access token raises the rate limit
# from 60 req/hour (unauthenticated) to 5000 req/hour. The token only
# needs the `public_repo` scope (or no scopes at all for fine-grained tokens).
GITHUB_TOKEN=

# Port the BFF listens on (default 4000)
PORT=4000
